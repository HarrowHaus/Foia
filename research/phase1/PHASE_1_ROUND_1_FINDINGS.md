# Phase 1 — Round 1 Findings and Checkpoint

Date: 2026-07-16. Corpus checkpoint: `chk_fac243aac6014a5a878df5a8d7a12b87`
(autogenerated stats in `round1_checkpoint_autogen.md`).

This report fills the assessment fields the autogenerated checkpoint leaves
blank, per the communication rules in `docs/WORK_MODE_HANDOFF.md`.

## What was completed

- Executed 20 logged queries across two rounds (13 round-1 signal-class
  queries + 7 round-2 harvested queries) against three institutionally
  independent no-key source families (OSTI, Internet Archive, Federal
  Register). All queries and result counts are in `query_log.jsonl`;
  normalized results in `round1_results.jsonl` / `round2_results.jsonl`.
- Deliberately imported four exact primary records from the top cluster:
  two CIA reprogramming PDFs plus their Internet-Archive OCR-text
  derivatives, preserving raw bytes, SHA-256 hashes, provenance, and
  extraction warnings. Hash audit: 6/6 documents OK.
- Recorded four leads (`leads.jsonl`), including two negatives.

## Strongest current signals

1. **CIA congressional budget-reprogramming / appropriations-oversight
   cluster, 1965–1988** (`lead_r1_reprogramming_oversight`). Four
   independent queries — "reprogramming of funds", "Office of Budget,
   Program Analysis and Manpower", "questions for the record HAC",
   "reprogramming action" — converged on a coherent, dense set of records
   (FY1983 reprogramming, FY1987 DIA SAFE reprogramming, FY-87 continuing
   resolution, HAC surveys and briefings). This is a genuine cluster, not
   an isolated document, and it is testable against independent families.

2. **Form 610 "Routing and Record Sheet" as a linkage substrate**
   (`lead_r1_routing_record_sheet`). Recurs across three decades; most
   valuable as network edges (originating/receiving office) rather than as
   a signal in itself.

## Strongest disconfirming evidence

- The reprogramming cluster's density is exactly what routine appropriations
  correspondence for a large agency would produce; the CIA Reading Room's
  completeness is a plausible sole cause. No anomaly is established — only a
  well-populated, testable region.
- Cross-family independence is **not yet demonstrated**: every cluster
  document comes from one family (Internet Archive's CIA mirror). Gate 1
  explicitly requires multiple independent families, so this round does not
  meet Gate 1.
- `lead_r1_synthetic_successor_artifact`: the one item that pattern-matched
  "successor program" is a single 2026 upload with no corroboration —
  weakened and not promoted.

## Dead ends / negatives

- `lead_r1_reprogramming_vocab_nontransfer`: "reprogramming" in OSTI means
  *metabolic* reprogramming, not budget. Lexical overlap across families is
  not semantic independence; recorded so later rounds don't miscount it.
- Round-1 Internet Archive queries "attachment removed", "destruction of
  records", and "destruction certificate" returned mostly unrelated media
  (audiobooks, VOA broadcasts, video). These broad phrases are low-yield in
  IA full-text search and should be replaced with exact form/notice titles.

## Source limitations

- NARA, CourtListener, govinfo, Congress are unavailable (no keys / contract
  unverified), so the independent second family needed to test the top
  cluster cannot yet be queried. This is the primary blocker to Gate 1.
- Internet Archive full-text queries are noisy across mixed media; precise
  identifiers or collection scoping are needed.

## Extraction limitations

- Both ingested CIA PDFs are image-only scans and produced no text (OCR
  stack absent — the Phase 0 limitation, now realized on real records). The
  workspace fell back to Internet Archive's own OCR-text derivative, which
  is searchable but is itself derivative and imperfect (visible OCR noise).
  Installing `pytesseract`/`PyMuPDF` is required before treating scanned
  PDFs as primary searchable text.

## Changes in confidence

- Raised confidence that the *system* works end-to-end on live records
  (search → deliberate import → provenance → hash audit → FTS → analysis).
- No increase in confidence about any *finding*; the top cluster is a
  promising testable region, not evidence of anything.

## Gate decision rationale

Gate 1 is **not** met: only one source family underlies the cluster, and
keyed families needed for independent corroboration are unavailable. This
round satisfies its own stopping criteria (two logged rounds, ranked
clusters, ordinary explanations recorded) but does not authorize Phase 2.

## Exact next-phase plan (Phase 1 continuation)

1. Obtain at least one keyed family (CourtListener token or NARA key) to
   test the reprogramming cluster against an independent source.
2. Install the OCR stack; re-extract the two scanned PDFs and compare
   extracted text against the IA OCR derivative.
3. Replace low-yield broad IA phrases with exact notice/form titles; add
   Federal Register records-schedule queries by exact title.
4. Run 2–3 more query rounds targeting a *different* signal class (e.g.
   cited-but-missing annexes; successor programs after formal termination)
   to find a second, independent cluster before considering any candidate
   file.
5. Re-checkpoint and re-evaluate Gate 1.

## Note on scope and minimization

All records are declassified U.S. government institutional documents
(budget/appropriations correspondence). No private-individual personal data
was collected or promoted. Nothing here asserts a conclusion; every item is
a lead pending corroboration and novelty checks.
